name: OMEGA Cloud Runner
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  run-simulation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run OMEGA
        run: python3 omega_core.py
